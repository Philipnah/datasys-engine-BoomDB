package dk.itu.boomdb;

/** Supported persistent column value types. */
public enum ColumnType {
    /** An ASCII string. */
    STRING,
    /** A signed 64-bit integer. */
    LONG,
    /** A double-precision IEEE 754 number. */
    DOUBLE
}
