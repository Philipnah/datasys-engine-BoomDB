package dk.itu.boomdb;

/** Comparisons supported by filtered scans. */
public enum Comparison {
    /** Equality comparison. */
    EQUALS,
    /** Strict less-than comparison. */
    LESS_THAN,
    /** Strict greater-than comparison. */
    GREATER_THAN
}
